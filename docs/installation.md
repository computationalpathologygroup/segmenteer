
For development installation with deep learning support, use the following command:

```bash
uv pip install -e ".[dl]"
```
For all dependencies used across the project, use:

```bash
pip install -e . ".[all]"
```